<script setup lang="ts">
import PageHeader from '@/pages/components/PageHeader.uvue'
</script>

<template>
	<view class="shell">
		<PageHeader title="服务说明" :showBack="true"></PageHeader>
		<scroll-view class="page" scroll-y="true">
			<view class="hero-card">
				<text class="eyebrow">账号服务</text>
				<text class="hero-title">如何使用题材查询</text>
				<text class="hero-copy">服务内容、开放范围、试吃规则和有效期将在正式发布后，以本页版本为准。</text>
				<view class="hero-meta">
					<text class="hero-meta-copy">版本：待发布</text>
					<text class="hero-meta-copy">更新时间：待定</text>
				</view>
			</view>

			<view class="section-heading">
				<view class="heading-mark"></view>
				<text class="heading-copy">使用步骤</text>
			</view>
			<view class="status-card">
				<view class="status-row">
					<view class="status-index"><text class="status-index-copy">1</text></view>
					<view class="status-copy-wrap">
						<text class="status-title">登录账号</text>
						<text class="status-copy">使用手机号验证码登录，并同意当前版本协议；正式文本发布前入口保持关闭。</text>
					</view>
				</view>
				<view class="status-row">
					<view class="status-index"><text class="status-index-copy">2</text></view>
					<view class="status-copy-wrap">
						<text class="status-title">查看服务状态</text>
						<text class="status-copy">会员有效期与试读次数以服务端返回为准，不使用本地示例状态。</text>
					</view>
				</view>
				<view class="status-row">
					<view class="status-index"><text class="status-index-copy">3</text></view>
					<view class="status-copy-wrap">
						<text class="status-title">使用兑换码</text>
						<text class="status-copy">站外获得的兑换码可在登录后激活；当前兑换码接口尚未开放。</text>
					</view>
				</view>
			</view>

			<view class="tip-card">
				<text class="tip-title">服务边界</text>
				<text class="tip-copy">页面不提供在线支付、收款码或交易入口；题材内容仍以公开来源和审核状态为前提，不提供对具体证券的评价、预测或交易建议。</text>
			</view>

			<view class="notice">
				<text class="notice-copy">本平台为公开信息聚合与历史数据统计，不构成任何投资建议。</text>
			</view>
		</scroll-view>
	</view>
</template>

<style scoped>
.shell,
.page {
	flex: 1;
}

.page {
	background-color: #f5f6f8;
}

.hero-card,
.status-card,
.tip-card {
	margin: 16px;
	padding: 20px;
	border-radius: 18px;
	background-color: #ffffff;
}

.hero-card {
	padding-top: 26px;
	padding-bottom: 26px;
}

.eyebrow {
	font-size: 13px;
	font-weight: 600;
	color: #e93030;
}

.hero-title {
	margin-top: 8px;
	font-size: 23px;
	font-weight: 700;
	color: #292d34;
}

.hero-copy,
.status-copy,
.tip-copy {
	margin-top: 9px;
	font-size: 14px;
	line-height: 21px;
	color: #757c86;
}

.hero-meta {
	margin-top: 14px;
	flex-direction: row;
	align-items: center;
}

.hero-meta-copy {
	flex: 1;
	font-size: 12px;
	color: #89909a;
}

.section-heading {
	margin: 22px 16px 12px;
	flex-direction: row;
	align-items: center;
}

.heading-mark {
	width: 4px;
	height: 18px;
	margin-right: 8px;
	border-radius: 2px;
	background-color: #e93030;
}

.heading-copy {
	font-size: 17px;
	font-weight: 700;
	color: #2c3037;
}

.status-card {
	margin-top: 0;
	padding: 0 20px;
	overflow: hidden;
}

.status-row {
	min-height: 82px;
	padding: 16px 0;
	flex-direction: row;
	align-items: flex-start;
	border-bottom-width: 1px;
	border-bottom-color: #f0f1f3;
}

.status-index {
	width: 28px;
	height: 28px;
	margin: 2px 12px 0 0;
	justify-content: center;
	align-items: center;
	border-radius: 14px;
	background-color: #fff0f0;
}

.status-index-copy {
	font-size: 14px;
	font-weight: 700;
	color: #e93030;
}

.status-copy-wrap {
	flex: 1;
}

.status-title,
.tip-title {
	font-size: 16px;
	font-weight: 600;
	color: #343942;
}

.status-copy {
	margin-top: 5px;
	font-size: 13px;
	line-height: 19px;
}

.tip-card {
	margin-top: 16px;
}

.notice {
	margin: 24px;
}

.notice-copy {
	font-size: 12px;
	line-height: 18px;
	color: #9aa1aa;
	text-align: center;
}
</style>
