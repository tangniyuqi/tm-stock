<script setup lang="ts">
import PageHeader from '@/pages/components/PageHeader.uvue'
</script>

<template>
	<view class="shell">
		<PageHeader title="登录" :showBack="true"></PageHeader>
		<scroll-view class="page" scroll-y="true">
			<view class="hero">
				<text class="eyebrow">账号服务</text>
				<text class="title">登录服务准备中</text>
				<text class="subtitle">账号服务将在隐私政策、用户协议、短信服务和后端能力完成后开放。</text>
			</view>

			<view class="form-card">
				<text class="form-title">手机号登录</text>
				<view class="field disabled-field">
					<text class="field-label">手机号</text>
					<input class="field-input" :disabled="true" placeholder="服务尚未开放" />
				</view>
				<view class="field disabled-field">
					<text class="field-label">验证码</text>
					<input class="field-input" :disabled="true" placeholder="服务尚未开放" />
					<text class="code-action">获取验证码</text>
				</view>
				<view class="agreement">
					<view class="checkbox"></view>
					<text class="agreement-copy">服务开放后，需阅读并同意正式发布的用户协议与隐私政策。</text>
				</view>
				<view class="login-button">
					<text class="login-button-copy">暂未开放登录</text>
				</view>
			</view>

			<view class="tip-card">
				<text class="tip-title">当前不会收集个人信息</text>
				<text class="tip-copy">页面仅展示后续登录流程的界面结构；手机号、验证码与提交操作均已关闭。</text>
			</view>

			<view class="notice">
				<text class="notice-copy">本平台为公开信息聚合与历史数据统计，不构成任何投资建议。</text>
			</view>
		</scroll-view>
	</view>
</template>

<style scoped>
.shell,
.page {
	flex: 1;
}

.page {
	background-color: #f5f6f8;
}

.hero {
	padding: 30px 20px 26px;
	background-color: #ffffff;
}

.eyebrow {
	font-size: 13px;
	font-weight: 600;
	color: #e93030;
}

.title {
	margin-top: 8px;
	font-size: 24px;
	font-weight: 700;
	color: #20242b;
}

.subtitle {
	margin-top: 9px;
	font-size: 14px;
	line-height: 22px;
	color: #737983;
}

.form-card,
.tip-card {
	margin: 16px;
	padding: 20px;
	border-radius: 18px;
	background-color: #ffffff;
}

.form-title,
.tip-title {
	font-size: 17px;
	font-weight: 700;
	color: #292d34;
}

.field {
	min-height: 54px;
	margin-top: 16px;
	padding: 0 14px;
	flex-direction: row;
	align-items: center;
	border-radius: 10px;
	background-color: #f7f8fa;
}

.field-label {
	width: 58px;
	font-size: 14px;
	color: #606773;
}

.field-input {
	flex: 1;
	font-size: 14px;
	color: #a0a5ad;
}

.code-action {
	min-width: 84px;
	font-size: 13px;
	color: #b4b8bf;
	text-align: right;
}

.agreement {
	margin-top: 18px;
	flex-direction: row;
	align-items: flex-start;
}

.checkbox {
	width: 16px;
	height: 16px;
	margin: 2px 8px 0 0;
	border-width: 1px;
	border-color: #c9cdd3;
	border-radius: 3px;
}

.agreement-copy,
.tip-copy {
	flex: 1;
	font-size: 12px;
	line-height: 19px;
	color: #8a9099;
}

.login-button {
	min-height: 48px;
	margin-top: 20px;
	justify-content: center;
	align-items: center;
	border-radius: 24px;
	background-color: #d9dce1;
}

.login-button-copy {
	font-size: 16px;
	font-weight: 600;
	color: #ffffff;
}

.tip-card {
	margin-top: 0;
}

.tip-copy {
	margin-top: 8px;
	font-size: 14px;
	line-height: 21px;
}

.notice {
	margin: 24px;
}

.notice-copy {
	font-size: 12px;
	line-height: 18px;
	color: #9ba1aa;
	text-align: center;
}
</style>
