<template>
  <view class="page">
    <view class="brand">
      <view class="brand-title">
        <text>注册账号</text>
      </view>
      <view class="brand-sub">
        <text>手机号 + 验证码，设置登录密码</text>
      </view>
    </view>

    <!-- 表单卡片 -->
    <view class="form-card">
      <view class="field">
        <x-input v-model="phone" placeholder="请输入手机号" round="12" type="tel" inputmode="numeric" :maxlength="11" leftIcon="phone-line" height="48"></x-input>
      </view>

      <view class="field">
        <view class="code-row">
          <x-input v-model="smsCode" class="code-input" placeholder="请输入验证码" round="12" type="number" inputmode="numeric" :maxlength="6" height="48"></x-input>
          <view class="get-code" :class="countdown > 0 ? 'get-code-disabled' : ''" @click="getSmsCode">
            <text class="get-code-text">{{ countdown > 0 ? countdown + 's后重发' : '获取验证码' }}</text>
          </view>
        </view>
      </view>

      <view class="field">
        <x-input v-model="password" placeholder="请设置密码（6-20位）" round="12" password leftIcon="lock-2-line" :maxlength="20" height="48"></x-input>
      </view>

      <view class="field">
        <x-input v-model="confirmPassword" placeholder="请再次确认密码" round="12" password leftIcon="lock-2-line" :maxlength="20" height="48"></x-input>
      </view>

      <view class="btn-wrap">
        <x-button color="#E93030" fontColor="#FFFFFF" :block="true" :round="28" :height="48" :loading="loading" :disabled="loading" @click="doRegister">注册</x-button>
      </view>

      <view class="login-row">
        <text class="tip-text">已有账号？</text>
        <view @click="goLogin">
          <text class="link-text">直接登录</text>
        </view>
      </view>
    </view>

    <view class="footer">
      <text class="footer-text">注册即表示同意《用户协议》与《隐私政策》</text>
    </view>
  </view>
</template>

<script lang="ts" setup>
import { ref, onBeforeUnmount } from 'vue'
import { api as authApi } from '@/api/auth.uts'

const phone = ref('')
const smsCode = ref('')
const password = ref('')
const confirmPassword = ref('')
const countdown = ref(0)
const loading = ref(false)

let intervalId : number = -1

const isValidPhone = (num : string) : boolean => {
	let n = (num || '').trim()
	if (n.length != 11) {
		return false
	}
	return n.charAt(0) == '1'
}

const isEmpty = (val : string) : boolean => {
	return (val || '').trim() == ''
}

const stopCountdown = () => {
	if (intervalId != -1) {
		clearInterval(intervalId)
		intervalId = -1
	}
}

const startCountdown = () => {
	countdown.value = 60
	intervalId = setInterval(() : void => {
		countdown.value = countdown.value - 1
		if (countdown.value <= 0) {
			stopCountdown()
		}
	}, 1000)
}

const getSmsCode = async () => {
	if (countdown.value > 0) {
		return
	}
	if (!isValidPhone(phone.value)) {
		uni.showToast({ title: '请输入正确的手机号', icon: 'none' })
		return
	}
	let ok = await authApi.sendCode(phone.value.trim(), 'register')
	if (ok) {
		uni.showToast({ title: '验证码已发送', icon: 'none' })
		startCountdown()
	}
}

const doRegister = async () => {
	if (loading.value) {
		return
	}
	if (!isValidPhone(phone.value)) {
		uni.showToast({ title: '请输入正确的手机号', icon: 'none' })
		return
	}
	if (isEmpty(smsCode.value)) {
		uni.showToast({ title: '请输入验证码', icon: 'none' })
		return
	}
	if (password.value.length < 6) {
		uni.showToast({ title: '密码不能少于6位', icon: 'none' })
		return
	}
	if (password.value != confirmPassword.value) {
		uni.showToast({ title: '两次输入的密码不一致', icon: 'none' })
		return
	}

	loading.value = true
	try {
		let resp = await authApi.register(
			phone.value.trim(),
			smsCode.value.trim(),
			password.value
		)
		if (resp != null) {
			uni.setStorageSync('token', resp.token)
			uni.showToast({ title: '注册成功', icon: 'success' })
			uni.switchTab({ url: '/pages/member/index' })
		}
	} finally {
		loading.value = false
	}
}

const goLogin = () => {
	uni.navigateTo({ url: '/pages/member/login' })
}

onBeforeUnmount(() => {
	stopCountdown()
})
</script>

<style scoped>
.page {
	min-height: 100vh;
	background: #F5F5F5;
	flex-direction: column;
	padding: 24px 16px 32px;
}

.brand {
	padding: 40px 0 28px;
}

.brand-title {
	font-size: 24px;
	font-weight: bold;
	color: #333333;
	padding-left: 4px;
}

.brand-sub {
	margin-top: 8px;
	font-size: 13px;
	color: #999999;
	padding-left: 4px;
}

.form-card {
	background: #FFFFFF;
	border-radius: 16px;
	padding: 24px 20px 20px;
	box-shadow: 0 10px 24px rgba(0, 0, 0, 0.06);
}

.field {
	margin-bottom: 16px;
}

.code-row {
	flex-direction: row;
	align-items: center;
}

.code-input {
	flex: 1;
}

.get-code {
	margin-left: 12px;
	height: 48px;
	padding: 0 18px;
	border-radius: 12px;
	background: #FFF0F0;
	align-items: center;
	justify-content: center;
	border: 1rpx solid #F7C7C7;
}

.get-code-disabled {
	background: #F5F5F5;
	border-color: #EEEEEE;
}

.get-code-text {
	font-size: 13px;
	color: #E93030;
}

.get-code-disabled .get-code-text {
	color: #999999;
}

.btn-wrap {
	padding: 8px 0 0;
}

.login-row {
	flex-direction: row;
	align-items: center;
	justify-content: center;
	margin-top: 18px;
}

.tip-text {
	font-size: 14px;
	color: #999999;
	margin-right: 2px;
}

.link-text {
	font-size: 14px;
	color: #E93030;
}

.footer {
	align-items: center;
	margin-top: 28px;
}

.footer-text {
	font-size: 12px;
	color: #999999;
}
</style>