<script setup lang="ts">
import PageHeader from '@/pages/components/PageHeader.uvue'

const openNews = () : void => {
	uni.switchTab({ url: '/pages/news/index' })
}
</script>

<template>
	<view class="shell">
		<PageHeader title="快讯详情" :showBack="true"></PageHeader>
		<scroll-view class="page" scroll-y="true">
			<view class="state-card">
				<view class="state-icon"><text class="state-icon-copy">讯</text></view>
				<text class="state-title">资讯服务尚未开放</text>
				<text class="state-copy">快讯详情仅在内容授权、来源核验和审核完成后才会提供。当前没有可展示的详情内容。</text>
				<text class="back-action" @click="openNews">返回快讯列表</text>
			</view>
			<view class="notice">
				<text class="notice-copy">本平台为公开信息聚合与历史数据统计，不构成任何投资建议。</text>
			</view>
		</scroll-view>
	</view>
</template>

<style scoped>
.shell, .page { flex: 1; }
.page { padding-bottom: 64px; background-color: #f5f6f8; }
.state-card { margin: 18px 16px; padding: 30px 20px; align-items: center; border-radius: 18px; background-color: #ffffff; }
.state-icon { width: 54px; height: 54px; justify-content: center; align-items: center; border-radius: 16px; background-color: #fff0f0; }
.state-icon-copy { font-size: 21px; font-weight: 700; color: #e93030; }
.state-title { margin-top: 15px; font-size: 17px; font-weight: 700; color: #30343b; text-align: center; }
.state-copy { margin-top: 9px; font-size: 14px; line-height: 22px; color: #737983; text-align: center; }
.back-action { min-height: 44px; margin-top: 16px; padding: 12px 16px; font-size: 14px; font-weight: 700; color: #e93030; }
.notice { margin: 24px; }
.notice-copy { font-size: 12px; line-height: 18px; color: #9aa1aa; text-align: center; }
</style>
