<script setup lang="ts">
import PageHeader from '@/pages/components/PageHeader.uvue'

const openHome = () : void => {
	uni.switchTab({ url: '/pages/index/index' })
}

const openThemeQuery = () : void => {
	uni.navigateTo({ url: '/pages/segment/index' })
}
</script>

<template>
	<view class="shell">
		<PageHeader title="资讯服务" :showBack="false"></PageHeader>
		<scroll-view class="page" scroll-y="true">
			<view class="hero">
				<view class="section-tabs">
					<view class="section-tab" @click="openHome"><text class="section-tab-copy">首页</text></view>
					<view class="section-tab section-tab-active"><text class="section-tab-copy active-copy">资讯</text></view>
					<view class="section-tab" @click="openThemeQuery"><text class="section-tab-copy">题材库</text></view>
				</view>
				<text class="eyebrow">内容服务</text>
				<text class="title">资讯快讯准备中</text>
				<text class="subtitle">仅在第三方内容授权、来源核验、原文链接与审核流程完整后，才会在这里展示公开资讯。</text>
			</view>

			<view class="state-card">
				<view class="state-icon"><text class="state-icon-copy">讯</text></view>
				<text class="state-title">当前暂无可发布快讯</text>
				<text class="state-copy">为避免无授权转载或缺少来源的内容进入服务，列表、搜索和详情目前保持关闭。不会用模拟资讯或其他内容填充。</text>
				<view class="state-rule">
					<text class="state-rule-copy">开放条件：来源名称、原文链接、发布时间与审核状态均完整。</text>
				</view>
			</view>

			<view class="notice">
				<text class="notice-copy">本平台为公开信息聚合与历史数据统计，不构成任何投资建议。</text>
			</view>
		</scroll-view>
	</view>
</template>

<style scoped>
.shell, .page { flex: 1; }
.page { padding-bottom: 64px; background-color: #f5f6f8; }
.hero { padding: 0 20px 24px; background-color: #ffffff; }
.section-tabs { height: 58px; flex-direction: row; align-items: center; }
.section-tab { min-width: 58px; height: 58px; margin-right: 28px; justify-content: center; align-items: center; border-bottom-width: 3px; border-bottom-color: transparent; }
.section-tab-copy { font-size: 16px; font-weight: 600; color: #60656e; }
.section-tab-active { border-bottom-color: #e93030; }
.active-copy { color: #e93030; }
.eyebrow { font-size: 12px; font-weight: 600; color: #e93030; }
.title { margin-top: 8px; font-size: 24px; font-weight: 700; color: #1f2329; }
.subtitle { margin-top: 8px; font-size: 14px; line-height: 21px; color: #6b7280; }
.state-card { margin: 18px 16px; padding: 28px 20px; align-items: center; border-radius: 18px; background-color: #ffffff; }
.state-icon { width: 54px; height: 54px; justify-content: center; align-items: center; border-radius: 16px; background-color: #fff0f0; }
.state-icon-copy { font-size: 21px; font-weight: 700; color: #e93030; }
.state-title { margin-top: 15px; font-size: 17px; font-weight: 700; color: #30343b; text-align: center; }
.state-copy { margin-top: 9px; font-size: 14px; line-height: 22px; color: #737983; text-align: center; }
.state-rule { margin-top: 18px; padding: 12px; border-radius: 12px; background-color: #fff8eb; }
.state-rule-copy { font-size: 12px; line-height: 18px; color: #89642b; text-align: center; }
.notice { margin: 24px; }
.notice-copy { font-size: 12px; line-height: 18px; color: #9aa1aa; text-align: center; }
</style>
