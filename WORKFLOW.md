# 研发流程闭环

> 本文件是流程定义；执行规范在 `.claude/agents/`，入口见 [CLAUDE.md](CLAUDE.md)。

## 流程（10 节点）

```
① 需求提出 👤  → ② 澄清·验收标准 → ③ 产品/页面设计 → ④ 测试设计
                                                          ↓
⑧ 上线确认 👤 ← ⑦ 质量门禁 ← ⑥ 代码审查 ← ⑤ 开发（前后端并行）
      ↓
⑨ 运维观测 → ⑩ 复盘提炼 ──(经验→规则)──> 回到 .claude/agents/
```

👤 = **必须人参与的卡点**（全流程仅 2 处：需求提出、上线确认）

| 节点 | 角色 | 输入 → 输出 | 通过标准 |
|------|-----|------------|---------|
| ① 需求提出 | **人** | 一句话诉求 | 说清要什么、为什么 |
| ② 澄清·验收 | pm-agent | 诉求 → `docs/specs/{需求}/requirements.md` | **人对验收标准点头**；挂得上 GOALS.md |
| ③ 设计 | pm/arch | 验收标准 → `design.md`（页面结构/字段/状态/空态） | 开发零追问可开工 |
| ④ 测试设计 | review-agent | 验收标准 → 用例清单 | 每条验收标准 ≥1 用例，含边界/异常 |
| ⑤ 开发 | back/web-agent | 设计 → 代码 | L1 hooks 零报错 |
| ⑥ 代码审查 | review-agent | 代码 vs spec | 无越界改动/硬编码/安全漏洞 |
| ⑦ 质量门禁 | 机器 | `bash scripts/harness-checks.sh` | **全绿才算完成，自述不算数** |
| ⑧ 上线确认 | **人** | 改动清单 + 风险项 | 人点头才发布 |
| ⑨ 运维观测 | 机器 | 监控告警 → runbook | 告警可按图索骥 |
| ⑩ 复盘提炼 | 全体 | 错误≥2次 → 变成规则/门禁 | 沉淀到 `.claude/agents/` 或自动检查 |

## 需求分级（哪些节点可跳过）

| 等级 | 特征 | ③设计 | ④用例 | ⑥审查 |
|-----|------|-------|-------|-------|
| A 标准 CRUD | 套模板增删改查 | 跳过 | 模板用例 | 简审 |
| B 新业务流程 | 新状态机/跨模块 | 必走 | 必走 | 必走 |
| C 性能敏感 | 大数据量/高并发 | 视情况 | 必走+压测 | 必走 |
| D 探索型 | 新方向/新模块 | 必走 | 必走 | 必走 |

## 工件分层（单一事实源）

```
Goal   为什么做 → docs/GOALS.md（当季 ≤3 条，需求过滤器）
 └─ Spec   做什么 → docs/specs/{需求}/（三件套，上线即归档）
     └─ Rules  怎么做 → .claude/agents/（规范与方法论）
         └─ Code/Test  做出来 → 四层护栏守护
             └─ ADR/坑点  沉淀 → docs/adr/ + .claude/agents/
```

**规则：知识只往底层沉淀，不滞留中间层；每类工件恰好一个权威位置，其余只引用不复制。**

## 治理（每周一次，不可委托给 AI）

30 分钟环境审查：门禁有没有被绕过（`--no-verify` 记录）、基线有没有变大、
文档与现实有没有漂移、已上线未归档的 spec。**审的就是 AI，所以不能让 AI 审自己。**
