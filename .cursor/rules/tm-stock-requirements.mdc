---
description: tm-stock 需求分析规则（写代码前必须完成的对齐）
alwaysApply: true
---

# tm-stock 需求分析规则

## 动手前必须完成

1. **澄清**：模棱两可处**列出所有可能性让人选**，不要自行假设。
2. **合规预检**：这个需求会不会碰到个股/预测/排序/荐股？
   碰到 → 先按 `tm-stock-compliance` 收敛或否掉，**不要先做了再说**。
3. **阶段对齐**：挂得上 `docs/GOALS.md` 当季目标吗？挂不上要提示"是否该现在做"。
4. **落盘**：`docs/specs/{需求}/requirements.md`（模板在 `docs/specs/_TEMPLATE/`）。

## 验收标准必须可测

- ❌ "体验流畅" → ✅ "列表首屏 p95 < 1.5s"
- ❌ "支持搜索" → ✅ "关键词 300ms 防抖返回；无结果显示空态文案 X"

必须覆盖：正常流 + **边界** + **异常态** + 权限（未登录/未订阅）。

## 需求分级（决定跳过哪些环节）

| 等级 | 特征 | 设计 | 用例 |
|-----|------|------|------|
| A | 套模板 CRUD | 跳过 | 模板 |
| B | 新业务流程/状态机 | 必走 | 必走 |
| C | 性能敏感 | 视情况 | 必走+压测 |
| D | 探索型 | 必走 | 必走 |

## 禁止

- ❌ 需求文档里写实现方案（那是设计阶段的事）
- ❌ 写"任务总结报告"
