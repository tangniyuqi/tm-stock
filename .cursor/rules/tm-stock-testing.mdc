---
description: tm-stock 测试与质量门禁规则（四层护栏、门禁不许常红、真跑验证）
alwaysApply: true
---

# tm-stock 测试与门禁规则

## 四层护栏

| 层 | 触发 | 内容 |
|----|-----|------|
| L1 | 每次改文件（自动 hook） | 明文密钥 + **合规禁用词** + Go 格式 |
| L2 | git commit / push | 秒级检查 / 编译测试（`bash scripts/git-hooks/install.sh` 安装） |
| L3 | 里程碑完成 | `bash scripts/harness-checks.sh` |
| L4 | 无人值守 | `tasks/` 队列 + 独立复验 |

## 铁律

1. **Agent 自述不算数，门禁绿灯才算数。**
2. **语法通过 ≠ 功能正确**：关键逻辑必须**真跑一遍**（造正例 + 反例各测一次）。
3. **门禁宁可关掉也不要常红**——长期红灯必被忽略，等于没有门禁。
4. **基线只拦新债**：`secret-scan-baseline.txt` 只减不增；
   **禁止为了过门禁而扩基线或从禁用词表删词**。
5. **绕过是异常**：`--no-verify` 不得常态化。

## Go 测试

- Service 层必须有单测；表驱动测试为默认写法。
- `go test ./... -race` 必须绿。

## 前端

uni-app x 无 CLI lint/构建 → 更依赖规范自觉 + 合规词门禁 + **在 HBuilderX 内实际编译验证**。
自测清单：375px 窄屏、弱网、三态（加载/空/错误）、点击区域。
